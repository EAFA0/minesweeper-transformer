[project]
name = "minesweeper-transformer"
version = "0.1.0"
description = "CNN + Transformer 混合架构扫雷 AI"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24",
    "torch>=2.0",
    "tqdm>=4.64",
    "ms-toollib>=0.7",
]
